// 测试 for 循环引用迭代

fn main() i32 {
    var arr: [i32: 3] = [1, 2, 3];
    
    for arr |&item| {
        *item = *item * 2;  // 修改每个元素，乘以2
    }
    
    if arr[0] != 2 || arr[1] != 4 || arr[2] != 6 {
        return 1;
    }
    
    return 0;
}

