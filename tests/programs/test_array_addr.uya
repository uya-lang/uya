// 测试程序：验证 &array[index] 代码生成
// 测试取数组元素地址的功能

// 全局数组变量用于测试返回指针（避免返回局部变量地址）
var buffer: [byte: 100] = [];

fn test_array_addr() &void {
    // 使用全局数组，避免返回局部变量地址
    // 测试 &array[index] 表达式 - 使用常量索引
    const result: &void = &buffer[10] as &void;
    
    return result;
}

fn main() i32 {
    const ptr: &void = test_array_addr();
    if ptr == null {
        return 1;
    }
    return 0;
}

