// 移动语义：结构体赋值/传参/返回为移动，移动后仅目标可用（规范 §12.5）
// 预期：main 返回 0

struct S {
  id: i32
}

fn take_s(s: S) i32 {
  return s.id;
}

fn main() i32 {
  const a: S = S{ id: 42 };
  const b: S = a;
  const x: i32 = take_s(b);
  if x != 42 {
    return 1;
  }
  return 0;
}
