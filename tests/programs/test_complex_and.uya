// 测试复杂的逻辑与运算符
fn main() i32 {
    const a: i32 = 10;
    const b: i32 = 20;
    const c: i32 = 30;
    
    // 复杂的逻辑与表达式
    if a == 10 && b == 20 && c == 30 {
        return 0;  // 成功
    }
    
    return 1;  // 失败
}