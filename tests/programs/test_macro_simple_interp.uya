// 测试宏内简单插值
mc test_add(a: expr, b: expr) expr {
    @mc_code(@mc_ast(${a} + ${b}));
}

fn main() i32 {
    const result: i32 = test_add(3, 5);
    if result != 8 {
        return 1;
    }
    return 0;
}

