// defer 在 continue 前执行；循环多轮时每轮块内 defer 各自执行
fn main() i32 {
    var x: i32 = 0;
    var n: i32 = 0;
    while (n < 2) {
        defer { x = x + 1; }
        n = n + 1;
        continue;          // 先执行 defer(x+=1)，再 continue
    }
    return 1 - (x / 2);    // 两轮各执行一次 defer，x=2 则返回 0
}
