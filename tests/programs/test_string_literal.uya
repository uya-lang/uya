// 测试字符串字面量支持
// 验证解析、类型检查、代码生成是否正确处理字符串字面量

// 声明外部 C 函数用于测试字符串字面量
extern fn strcmp(s1: *byte, s2: *byte) i32;

// 测试函数：使用字符串字面量作为 extern 函数参数
fn main() i32 {
    // 字符串字面量作为 extern 函数参数
    // 注意：字符串字面量类型为 *byte（FFI 指针类型）
    const result: i32 = strcmp("hello", "world");
    
    // 如果能够成功编译并调用函数，说明字符串字面量支持已实现
    // 返回 0 表示测试通过（无论 strcmp 返回值是什么）
    return 0;
}

