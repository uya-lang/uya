// 测试 type 参数类型
// 规范 25.2：type 参数类型用于接收类型标识符

mc with_type_param(T: type) expr {
    @mc_code(@mc_ast(0));
}

fn main() i32 {
    // 调用带 type 参数的宏
    const x: i32 = with_type_param(i32);
    if x != 0 { return 1; }
    return 0;
}

