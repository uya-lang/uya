// 一元表达式测试
// 测试一元运算符（!, -）

fn main() i32 {
    const x: i32 = 10;
    const neg_x: i32 = -x;
    const not_true: bool = !true;
    const not_false: bool = !false;
    
    if neg_x == -10 && not_true == false && not_false == true {
        return 0;
    }
    return 1;
}

