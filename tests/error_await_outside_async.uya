// 错误测试：@await 只能在 @async_fn 函数中使用
// 这个测试应该编译失败

// 普通 async 函数
@async_fn
fn async_get_value() i32 {
    return 42;
}

// 普通函数中不应该允许 @await（编译应该报错）
fn non_async_function() i32 {
    const val: i32 = @await async_get_value();
    return val;
}

fn main() i32 {
    return 0;
}
