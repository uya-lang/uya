export fn main() i32 {
    var acc: i32 = 0;
    var i: i32 = 0;
    while i < 3 {
        if i < 2 {
            acc = acc + 2;
        } else {
            acc = acc + 1;
        }
        i = i + 1;
    }
    return acc;
}
