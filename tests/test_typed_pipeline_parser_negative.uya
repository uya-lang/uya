// test_typed_pipeline_parser_negative.uya
// parser 负向测试：验证非法 pipeline 表达式会被 parser 拒绝

fn pipeline() i32 { return 0; }
fn cmd(a: &byte) i32 { return 0; }
fn main() i32 {
    // 右侧不是函数调用
    _ = pipeline() |> y + z;
    return 0;
}
