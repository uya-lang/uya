// test_typed_pipeline_imported_transformer_positive.uya
// checker 正向测试：验证从其他模块导入的无 receiver 静态 transformer 可用作 '|>' 右侧。

use std.process;
use pipe_mod.pipeline_transformer;

fn main() i32 {
    _ = pipeline() |> pipeline_transformer();
    return 0;
}
