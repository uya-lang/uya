---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-02-16T08:00:00.000Z
provider: 
---

# Uya 开发流程规则

## TDD 开发流程（推荐）

新功能开发时，遵循测试驱动开发：

```bash
# 1. 先写测试（测试文件放在 tests/programs/）
vim tests/programs/test_new_feature.uya

# 2. 运行测试，确认失败（红）
make tests-uya  # 新测试应该失败

# 3. 编写最小代码使测试通过（绿）
vim src/related_module.uya

# 4. 验证和备份（一步完成）
make backup
```

#### TDD 核心原则

- ✅ **红-绿-重构**：先写失败测试，再写代码，最后重构
- ✅ **小步迭代**：每次只添加一个小功能点
- ✅ **测试先行**：任何新功能必须有对应测试

## 开发前准备

### 阅读语言规范

在开始开发前，必须阅读语言规范文档：

```bash
# 阅读 Uya 语言完整语法（AI 提示词版）
cat docs/uya_ai_prompt.md
```

**关键内容**：
- 类型系统、内存布局
- 宏系统（`mc` 关键字）
- 内置函数（`@size_of`、`@mc_eval` 等）
- 语法规则

### 学习开发技能

```bash
# 阅读 Uya 开发技能文档
cat .codebuddy/skills/uya-development.md
```

### 理解 Uya 语法规则

开发时必须遵循 Uya 语法规则，**不要瞎编乱造**：

- `str_equals(a, b) != 0` 表示字符串相等（返回 1 = 相等）
- Union 变体不能包含引用类型 `&T`
- 参考 `tests/programs/` 中的测试文件学习语法

## 开发前验证

在开始任何代码修改前，必须验证当前状态：

```bash
# 检查编译器是否存在
if [ ! -f bin/uya ]; then
    make from-c  # 从 C99 代码构建
fi

# 验证当前状态（自举 + 测试 + 备份）
make backup
```

## 开发后验证（推荐流程）

**使用 `make backup` 作为主要验证命令**，它会自动执行：

```bash
make backup     # 一键验证和备份
```

这会依次执行：
1. 自举验证（`make b`）- 编译器编译自身，验证输出一致性
2. 测试验证（`make tests-uya`）- 运行所有测试
3. 备份（更新 `backup/uya.c.zip`）

### 分步验证（可选）

如果需要单独验证某个步骤：

```bash
make uya        # 构建自举编译器 + 更新 bin/uya.c
make b          # 自举验证
make tests-uya  # 测试验证
```

## 关键命令速查

| 命令 | 说明 |
|------|------|
| `make backup` | 完整验证 + 备份（**推荐**） |
| `make uya` | 构建自举编译器 |
| `make b` | 自举验证 |
| `make tests-uya` | 测试验证 |
| `make from-c` | 从备份恢复并构建 |

## uya.c 备份机制

`bin/uya.c` 是自举编译器的种子文件，会被 `make clean` 删除。

### 备份条件

**只有 `make backup` 才会备份**，且必须满足：
1. 自举验证通过（`make b`）
2. 测试全部通过（`make tests-uya`）

```bash
make backup     # 验证通过后备份（推荐：开发完成后执行）
```

### 自动恢复

`make from-c` 时如果 `bin/uya.c` 不存在，自动从备份恢复

### 手动操作

```bash
make backup     # 验证 + 备份（自举和测试都通过才执行）
make restore    # 从备份恢复 bin/uya.c
make clean && make from-c  # 清理后从备份恢复构建
```

## 关键文件

| 文件 | 说明 |
|------|------|
| `src/*.uya` | 编译器源代码（唯一维护源） |
| `bin/uya.c` | 自举 C99 代码（种子文件） |
| `tests/programs/*.uya` | 测试文件 |

## 禁止操作

- ❌ 不要修改 `compiler-c/` 目录（已退役）
- ❌ 不要跳过自举验证（`make b`）
- ❌ 不要在测试失败时提交代码
- ❌ 不要删除有意义的测试（测试是回归保护）
- ❌ 不要瞎编乱造语法（参考现有代码和测试）

## 开发经验积累

每次开发获得的有用经验，应更新到技能文档：

```bash
# 更新技能文档
vim .codebuddy/skills/uya-development.md
```

经验积累示例：
- 新发现的语法规则
- 常见陷阱和解决方案
- 代码模式最佳实践

## 自举验证失败处理

如果 `make b` 失败：
1. 检查修改是否影响代码生成顺序
2. 排序相关的输出必须稳定
3. 确保 err_union 等类型按名称排序

## 栈限制问题

编译器需要较大栈空间，如果出现段错误：

```bash
# 手动增大栈限制
ulimit -s 65536

# 然后重新执行
make backup
```

`compile.sh` 已内置 `ulimit -s 65536` 设置。
