// 测试数组切片功能

fn main() i32 {
    // 声明并初始化一个数组变量
    const arr: [i32; 5] = [1, 2, 3, 4, 5];
    
    // 直接使用切片语法访问子数组
    // 注意：这里不尝试将切片赋值给变量，只测试切片访问
    const val: i32 = arr[0:2][0];  // 创建切片，然后访问切片的第一个元素
    
    // 验证结果
    if val != 1 {
        return 1;  // 测试失败
    }
    return 0;  // 测试成功
}