// 更复杂的测试用例
struct Point {
    x: i32,
    y: i32
}

fn add(a: i32, b: i32) i32 {
    return a + b;
}

fn main() i32 {
    const x: i32 = 5;
    const y: i32 = 10;
    const sum: i32 = add(x, y);
    return sum;
}