// 指针的简单示例

fn main() i32 {
    // 声明一个整数变量
    mut x: i32 = 42;
    
    // 获取x的指针
    const ptr: *i32 = &x;
    
    // 通过指针访问和修改x的值
    *ptr = 100;
    
    // 验证修改结果
    if x != 100 {
        return 1;  // 测试失败
    }
    
    return 0;  // 测试成功
}