// 测试 defer 与 return 的顺序
fn main() i32 {
    // 这个函数的主要目的是测试 defer 块是否在 return 语句之前执行
    defer {
        // 这个 defer 块应该在 return 之前执行
        // 我们将通过生成的 C 代码来验证
    }
    
    // 正常返回
    return 0;
}