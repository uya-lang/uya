enum Color {
    Red = 1,
    Green = 2,
    Blue = 3
}

fn main() i32 {
    const color: Color = Color.Red;
    const result: i32 = match color {
        Color.Red => 1,
        Color.Green => 2,
        Color.Blue => 3,
        else => 0
    };
    return result;
}

