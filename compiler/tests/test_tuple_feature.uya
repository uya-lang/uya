// Test tuple feature
fn main() i32 {
    const pair: (i32, bool) = (42, true);
    const triple: (i32, f64, bool) = (10, 3.14, false);
    
    // Access tuple elements
    if pair.0 == 42 {
        return 0;
    } else {
        return 1;
    }
}