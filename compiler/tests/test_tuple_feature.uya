// Test tuple feature
fn main() i32 {
    var pair (i32, bool) = (42, true);
    var triple (i32, f64, bool) = (10, 3.14, false);
    
    // Access tuple elements (when implemented)
    // var first i32 = pair.0;
    // var second bool = pair.1;
    
    if pair.0 == 42 {
        return 0;
    } else {
        return 1;
    }
}