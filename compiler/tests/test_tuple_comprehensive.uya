// Comprehensive tuple feature test
fn main() i32 {
    // Test 1: Basic tuple declaration and initialization
    const pair: (i32, bool) = (42, true);
    
    // Test 2: Tuple with multiple types
    const triple: (i32, f64, bool) = (10, 3.14, false);
    
    // Test 3: Tuple field access in expressions (type inference will be improved later)
    if pair.0 == 42 && pair.1 == true {
        if triple.0 == 10 && triple.1 == 3.14 && triple.2 == false {
            return 0;  // All tests passed
        }
    }
    
    return 1;  // Test failed
}

