// 测试extern声明
extern i32 printf(byte* fmt, ...);
extern f64 sin(f64);

struct Point {
    x: f32,
    y: f32
}

fn add(a: i32, b: i32) i32 {
    return a + b;
}

fn main() i32 {
    var x: i32 = 10;
    const y: i32 = 20;
    const sum: i32 = add(x, y);
    printf("Sum is: %d\n", sum);
    return 0;
}