    // 声明 3x4 的未初始化 i32 二维数组
    var matrix: [[i32: 4]; 3] = [];
    
    // 手动初始化每个元素
    var i: i32 = 0;
    while i < 3 {
        var j: i32 = 0;
        while j < 4 {
            matrix[i][j] = i * 4 + j;  // 需要边界检查证明
            j = j + 1;
        }
        i = i + 1;
    }
    