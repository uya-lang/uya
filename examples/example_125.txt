| 位置 | 作用 |
|---|---|
| `mc` | 内置空结构体名，**非关键字** |
| 返回标签 | `expr` / `stmt` / `struct` / `type` （同隐式宏） |

------------------------------------------------
2. 示例：一眼分清宏与函数
------------------------------------------------
**对比展示**（注意：实际使用中不能同时存在同名）：

