// 基本泛型函数测试
// 注意：当前泛型功能尚未实现，此测试用例用于验证实现

// 基本泛型函数：比较两个值并返回较大者
fn max<T: Ord>(a: T, b: T) T {
    if a > b {
        return a;
    }
    return b;
}

fn main() i32 {
    // 使用示例（Parser 应能解析，但 Checker 和 Codegen 尚未实现）
    // const x: i32 = max<i32>(10, 20);  // 返回 20
    // if x != 20 { return 1; }
    
    return 0;
}

