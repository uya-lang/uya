// 测试预期失败：type 返回类型的宏（当前不支持）
// 规范 25.5：type 返回类型用于生成类型标识符

mc get_type() type {
    @mc_code(@mc_ast(i32));
}

fn main() i32 {
    // 调用 type 返回类型的宏以触发错误
    const x: get_type() = 42;
    return x;
}

