// 结构体内定义方法（void 返回）
// 预期：main 返回 0

struct Counter {
  value: i32,
  fn inc(self: *Self) void {
    self.value = self.value + 1;
  }
}

fn main() i32 {
  var c: Counter = Counter{ value: 5 };
  c.inc();
  c.inc();
  if c.value != 7 {
    return 1;
  }
  return 0;
}
