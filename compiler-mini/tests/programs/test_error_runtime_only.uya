// 运行时错误：无需预声明，直接使用 error.X
fn f() !i32 {
    return error.NoDeclaration;  // 无 error NoDeclaration; 声明
}
fn main() !i32 {
    const r: i32 = f() catch { 99; };
    if (r != 99) return 1;
    return 0;
}
