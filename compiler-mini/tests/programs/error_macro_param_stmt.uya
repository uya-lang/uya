// 测试预期失败：stmt 参数类型（当前不支持带参数宏）
// 规范 25.2：stmt 参数类型用于接收语句

mc with_stmt_param(s: stmt) expr {
    @mc_code(@mc_ast(0));
}

fn main() i32 {
    // 调用带参数的宏以触发错误
    const x: i32 = with_stmt_param(return);
    return x;
}

