// 测试 use 语句基本功能（单文件场景，模拟模块导入）
// 注意：当前实现可能不支持多文件模块，此测试用于验证语法解析

fn helper() i32 {
    return 10;
}

fn main() i32 {
    // 在单文件场景下，use 语句应该被解析但不影响可见性
    // 实际模块系统需要多文件支持
    const val: i32 = helper();  // 验证函数调用正常
    if val != 10 {
        return 1;  // 验证失败
    }
    return 0;
}

