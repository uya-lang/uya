// str_utils.uya - 字符串辅助函数模块
// 封装 C 标准库字符串函数，简化 Uya Mini 代码中的字符串操作
//
// 注意：C 标准库函数声明在 extern_decls.uya 中
// 编译时需要包含 extern_decls.uya 文件

// ===== 封装函数（可选，提供更友好的接口）=====

// 字符串相等判断
fn str_equals(s1: &byte, s2: &byte) bool {
    return strcmp(s1 as *byte, s2 as *byte) == 0;
}

// 字符串长度（类型为 i32）
fn str_length(s: &byte) i32 {
    return strlen(s as *byte);
}

