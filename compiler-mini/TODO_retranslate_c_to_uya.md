# C 版本代码重新翻译到自举版本 TODO

> **⚠️ 重要提示：重新覆盖式翻译** 
> 
> **本翻译任务采用重新覆盖式翻译策略，而非增量式修改：**
> 1. **完全忽略现有 Uya 版本的实现**：不要基于现有 `uya-src/` 目录下的代码进行修改
> 2. **完全按照 C 版本重新翻译**：以 `src/` 目录下的 C 代码为唯一参考，从头开始翻译
> 3. **覆盖式替换**：翻译时应该完全覆盖现有的 Uya 版本代码，而不是在现有代码基础上修改
> 4. **在开始翻译之前，必须先阅读 `spec/UYA_MINI_SPEC.md` 语法规范**，了解 Uya Mini 的完整语法、类型系统和语义规则
> 5. **⚠️ 完成任务后必须及时更新 TODO**：每完成一个函数或文件的翻译，立即更新本文档中对应的检查框（`[ ]` → `[x]`）和状态标记，确保进度跟踪准确

---

## 📚 翻译前准备

### 1. 阅读语法规范（必须）

**文件路径：** `compiler-mini/spec/UYA_MINI_SPEC.md`

**重点关注章节：**
- 类型系统（基本类型、指针、数组、结构体、枚举）
- 表达式类型推断规则
- 类型转换规则
- 变量声明和初始化规则
- 函数声明和调用规则
- 控制流语句规则

### 2. 理解翻译原则

> **⚠️ 核心原则：重新覆盖式翻译**
> 
> **翻译时必须遵循以下原则：**
> 1. **完全按照 C 版本逻辑重新翻译**：
>    - **不要参考现有 Uya 版本的实现**
>    - **不要基于现有 Uya 版本进行修改**
>    - **完全以 C 版本为唯一参考，从头开始翻译**
>    - 保持与 C 版本完全一致的行为
> 2. **排除 C99 相关代码**：不翻译 `codegen/c99/` 目录下的代码
> 3. **保持函数签名一致**：函数名、参数、返回值类型必须一致
> 4. **保持错误处理逻辑一致**：错误报告、放宽检查的逻辑必须一致
> 5. **注意类型系统差异**（在翻译时进行转换，但逻辑必须一致）：
   - C 版本使用 `union`，Uya 版本使用结构体包含所有字段
   - C 版本使用 `->` 访问指针成员，Uya 版本使用 `.` 访问
   - C 版本使用 `*` 解引用，Uya 版本直接访问
   - **⚠️ Uya 不支持指针运算**：必须将 `ptr + i` 转换为 `&ptr[i]`
   - **⚠️ Uya 不支持函数前向声明**：必须重新组织函数顺序或使用函数表机制

### 3. 翻译检查清单

对于每个函数，检查：
- [ ] 函数签名是否一致（参数类型、返回值类型）
- [ ] 逻辑流程是否一致
- [ ] 错误处理是否一致
- [ ] 放宽检查的逻辑是否一致
- [ ] 类型推断逻辑是否一致
- [ ] 符号表操作是否一致
- [ ] 函数表操作是否一致

---

## 📋 需要翻译的文件列表

### 核心模块（优先级：P0）

#### 1. `src/checker.c` → `uya-src/checker.uya`
**状态：** ⚠️ 需要重新覆盖式翻译
**优先级：** P0（最高）
**说明：** 类型检查器核心模块，当前存在 `array_access` 等测试失败问题
**⚠️ 翻译策略：** 完全按照 C 版本重新覆盖式翻译，不要参考现有 Uya 版本的实现

**需要检查的函数（按调用顺序）：**

**公共接口函数：**
- [ ] `checker_init` - 初始化 TypeChecker
- [ ] `checker_get_error_count` - 获取错误计数
- [ ] `checker_check` - 类型检查主函数（两遍检查）

**错误报告：**
- [ ] `checker_report_error` - 报告类型检查错误

**类型系统核心函数：**
- [ ] `type_from_ast` - 从 AST 类型节点创建 Type 结构
- [ ] `type_equals` - 比较两个类型是否相等
- [ ] `type_can_implicitly_convert` - 检查类型是否可以隐式转换

**类型推断：**
- [ ] `checker_infer_type` - 推断表达式的类型（核心函数）

**符号表和函数表：**
- [ ] `hash_string` - 字符串哈希函数
- [ ] `symbol_table_lookup` - 在符号表中查找符号
- [ ] `symbol_table_insert` - 向符号表插入符号
- [ ] `function_table_lookup` - 在函数表中查找函数签名
- [ ] `function_table_insert` - 向函数表插入函数签名

**作用域管理：**
- [ ] `checker_enter_scope` - 进入新的作用域
- [ ] `checker_exit_scope` - 退出当前作用域

**AST 节点查找：**
- [ ] `find_struct_decl_from_program` - 在程序节点中查找结构体声明
- [ ] `find_enum_decl_from_program` - 在程序节点中查找枚举声明
- [ ] `find_struct_field_type` - 查找结构体字段的类型

**类型检查函数（按调用顺序）：**
- [ ] `checker_check_node` - 检查 AST 节点（主入口，递归调用其他检查函数）
- [ ] `checker_register_fn_decl` - 注册函数声明（第一遍检查）
- [ ] `checker_check_fn_decl` - 检查函数声明（第二遍检查）
- [ ] `checker_check_var_decl` - 检查变量声明（已重写，需验证）
- [ ] `checker_check_struct_decl` - 检查结构体声明
- [ ] `checker_check_expr_type` - 检查表达式类型是否匹配预期类型
- [ ] `checker_check_array_access` - 检查数组访问表达式
- [ ] `checker_check_member_access` - 检查成员访问表达式
- [ ] `checker_check_call_expr` - 检查函数调用表达式
- [ ] `checker_check_binary_expr` - 检查二元表达式
- [ ] `checker_check_unary_expr` - 检查一元表达式
- [ ] `checker_check_cast_expr` - 检查类型转换表达式
- [ ] `checker_check_struct_init` - 检查结构体初始化表达式
- [ ] `checker_check_alignof` - 检查 alignof 表达式
- [ ] `checker_check_len` - 检查 len 表达式

**参考文件：**
- C 版本：`compiler-mini/src/checker.c`
- 自举版本：`compiler-mini/uya-src/checker.uya`
- 语法规范：`compiler-mini/spec/UYA_MINI_SPEC.md`

#### 2. `src/parser.c` → `uya-src/parser.uya`
**状态：** ⚠️ 需要检查
**优先级：** P0（最高）
**说明：** 语法分析器，负责将词法分析结果转换为 AST

**需要检查的函数：**
- [ ] `parser_parse` - 解析主函数
- [ ] `parser_parse_program` - 解析程序
- [ ] `parser_parse_decl` - 解析声明
- [ ] `parser_parse_fn_decl` - 解析函数声明
- [ ] `parser_parse_struct_decl` - 解析结构体声明
- [ ] `parser_parse_enum_decl` - 解析枚举声明
- [ ] `parser_parse_var_decl` - 解析变量声明
- [ ] `parser_parse_type` - 解析类型
- [ ] `parser_parse_stmt` - 解析语句
- [ ] `parser_parse_expr` - 解析表达式
- [ ] `parser_parse_binary_expr` - 解析二元表达式
- [ ] `parser_parse_unary_expr` - 解析一元表达式
- [ ] `parser_parse_call_expr` - 解析函数调用
- [ ] `parser_parse_array_literal` - 解析数组字面量
- [ ] `parser_parse_struct_init` - 解析结构体初始化
- [ ] 其他解析函数...

**参考文件：**
- C 版本：`compiler-mini/src/parser.c`
- 自举版本：`compiler-mini/uya-src/parser.uya`

#### 3. `src/lexer.c` → `uya-src/lexer.uya`
**状态：** ⚠️ 需要检查
**优先级：** P0（最高）
**说明：** 词法分析器，负责将源代码转换为词法单元

**需要检查的函数：**
- [ ] `lexer_init` - 初始化词法分析器
- [ ] `lexer_next_token` - 获取下一个词法单元
- [ ] `lexer_peek_token` - 预览下一个词法单元
- [ ] `lexer_consume_token` - 消费词法单元
- [ ] `lexer_read_string` - 读取字符串字面量
- [ ] `lexer_read_number` - 读取数字字面量
- [ ] `lexer_read_identifier` - 读取标识符
- [ ] 其他词法分析函数...

**参考文件：**
- C 版本：`compiler-mini/src/lexer.c`
- 自举版本：`compiler-mini/uya-src/lexer.uya`

#### 4. `src/ast.c` → `uya-src/ast.uya`
**状态：** ⚠️ 需要检查
**优先级：** P0（最高）
**说明：** AST 节点创建和操作

**需要检查的函数：**
- [ ] `ast_new_node` - 创建新节点
- [ ] `ast_merge_programs` - 合并程序节点
- [ ] 其他 AST 操作函数...

**参考文件：**
- C 版本：`compiler-mini/src/ast.c`
- 自举版本：`compiler-mini/uya-src/ast.uya`

#### 5. `src/arena.c` → `uya-src/arena.uya`
**状态：** ⚠️ 需要检查
**优先级：** P0（最高）
**说明：** 内存分配器（Arena）

**需要检查的函数：**
- [ ] `arena_init` - 初始化 Arena
- [ ] `arena_alloc` - 分配内存
- [ ] `arena_reset` - 重置 Arena
- [ ] 其他 Arena 操作函数...

**参考文件：**
- C 版本：`compiler-mini/src/arena.c`
- 自举版本：`compiler-mini/uya-src/arena.uya`

#### 6. `src/main.c` → `uya-src/main.uya`
**状态：** ⚠️ 需要检查
**优先级：** P0（最高）
**说明：** 编译器主程序

**需要检查的函数：**
- [ ] `main` - 主函数
- [ ] 其他主程序函数...

**参考文件：**
- C 版本：`compiler-mini/src/main.c`
- 自举版本：`compiler-mini/uya-src/main.uya`

### LLVM C API 代码生成模块（优先级：P1）

#### 7. `src/codegen/llvm_capi/main.c` → `uya-src/codegen/llvm_capi/main.uya`
**状态：** ⚠️ 需要检查
**优先级：** P1
**说明：** LLVM 代码生成主入口

#### 8. `src/codegen/llvm_capi/init.c` → `uya-src/codegen/llvm_capi/init.uya`
**状态：** ⚠️ 需要检查
**优先级：** P1
**说明：** LLVM 初始化

#### 9. `src/codegen/llvm_capi/function.c` → `uya-src/codegen/llvm_capi/function.uya`
**状态：** ⚠️ 需要检查
**优先级：** P1
**说明：** 函数代码生成

#### 10. `src/codegen/llvm_capi/expr.c` → `uya-src/codegen/llvm_capi/expr.uya`
**状态：** ⚠️ 需要检查
**优先级：** P1
**说明：** 表达式代码生成

#### 11. `src/codegen/llvm_capi/stmt.c` → `uya-src/codegen/llvm_capi/stmt.uya`
**状态：** ⚠️ 需要检查
**优先级：** P1
**说明：** 语句代码生成

#### 12. `src/codegen/llvm_capi/types.c` → `uya-src/codegen/llvm_capi/types.uya`
**状态：** ⚠️ 需要检查
**优先级：** P1
**说明：** 类型代码生成

#### 13. `src/codegen/llvm_capi/structs.c` → `uya-src/codegen/llvm_capi/structs.uya`
**状态：** ⚠️ 需要检查
**优先级：** P1
**说明：** 结构体代码生成

#### 14. `src/codegen/llvm_capi/enums.c` → `uya-src/codegen/llvm_capi/enums.uya`
**状态：** ⚠️ 需要检查
**优先级：** P1
**说明：** 枚举代码生成

#### 15. `src/codegen/llvm_capi/global.c` → `uya-src/codegen/llvm_capi/global.uya`
**状态：** ⚠️ 需要检查
**优先级：** P1
**说明：** 全局变量代码生成

#### 16. `src/codegen/llvm_capi/vars.c` → `uya-src/codegen/llvm_capi/vars.uya`
**状态：** ⚠️ 需要检查
**优先级：** P1
**说明：** 变量代码生成

#### 17. `src/codegen/llvm_capi/funcs.c` → `uya-src/codegen/llvm_capi/funcs.uya`
**状态：** ⚠️ 需要检查
**优先级：** P1
**说明：** 函数相关辅助函数

#### 18. `src/codegen/llvm_capi/utils.c` → `uya-src/codegen/llvm_capi/utils.uya`
**状态：** ⚠️ 需要检查
**优先级：** P1
**说明：** 工具函数

### 辅助模块（优先级：P2）

#### 19. `src/str_utils.c` → `uya-src/str_utils.uya`（如果存在）
**状态：** ⚠️ 需要检查
**优先级：** P2
**说明：** 字符串工具函数

---

## 🛠️ 翻译工具使用说明

### 使用 AI 辅助翻译

当使用 AI（如 Cursor）进行翻译时，请遵循以下步骤：

1. **提供上下文**：
   ```
   我需要将 C 版本的函数翻译成 Uya 版本。
   - C 版本文件：compiler-mini/src/checker.c
   - 自举版本文件：compiler-mini/uya-src/checker.uya
   - 语法规范：compiler-mini/spec/UYA_MINI_SPEC.md
   - 目标函数：checker_check_var_decl
   ```

2. **要求 AI 进行重新覆盖式翻译**：
   ```
   ⚠️ 重要：采用重新覆盖式翻译策略
   
   请先阅读 compiler-mini/spec/UYA_MINI_SPEC.md 中的类型系统章节，
   然后完全按照 C 版本 compiler-mini/src/checker.c 中的 checker_check_var_decl 函数，
   从头开始重新翻译，完全覆盖 compiler-mini/uya-src/checker.uya 中的现有实现。
   
   注意：
   - 不要参考现有 Uya 版本的实现
   - 不要基于现有代码进行修改
   - 完全以 C 版本为唯一参考，从头开始翻译
   - 翻译完成后，完全覆盖现有的 Uya 版本代码
   ```

3. **提供重新覆盖式翻译指令**：
   ```
   ⚠️ 重新覆盖式翻译任务
   
   目标：将 C 版本函数完全重新翻译为 Uya 版本
   - C 版本参考：compiler-mini/src/checker.c 中的 checker_check_var_decl 函数
   - 目标文件：compiler-mini/uya-src/checker.uya
   - 语法规范：compiler-mini/spec/UYA_MINI_SPEC.md
   
   要求：
   - 完全按照 C 版本的逻辑重新翻译（不要参考现有 Uya 版本）
   - 函数签名必须一致
   - 逻辑流程必须一致
   - 错误处理必须一致
   - 放宽检查的逻辑必须一致
   - 翻译完成后，完全覆盖现有的 Uya 版本实现
   ```

### 翻译检查命令

```bash
# 1. 编译自举编译器
cd /media/winger/_dde_home/winger/uya/compiler-mini/uya-src
./compile.sh -e --c99

# 2. 运行测试
cd /media/winger/_dde_home/winger/uya/compiler-mini
./tests/run_programs.sh --uya

# 3. 对比 C 版本和自举版本的输出
./build/compiler-mini tests/programs/<test>.uya -o /tmp/test_c.c --c99
./build/uya-compiler/compiler tests/programs/<test>.uya -o /tmp/test_uya.c
diff /tmp/test_c.c /tmp/test_uya.c
```

---

## 🔄 翻译工作流程

### 步骤 1：选择要翻译的文件

1. 从文件列表中选择一个文件（建议按优先级顺序）
2. 确认该文件不在 `codegen/c99/` 目录下（C99 代码不需要翻译）

### 步骤 2：阅读 C 版本代码

1. 打开 C 版本文件（如 `src/checker.c`）
2. 理解代码逻辑和功能
3. 识别关键函数和数据结构
4. 理解错误处理和放宽检查的逻辑

### 步骤 3：阅读语法规范

1. 打开 `spec/UYA_MINI_SPEC.md`
2. 阅读相关的语法规则和类型系统规则
3. 理解类型推断规则和类型转换规则
4. 确认代码实现是否符合规范

### 步骤 4：重新覆盖式翻译（⚠️ 重要）

> **⚠️ 注意：本步骤采用重新覆盖式翻译，而非对比修改**
> 
> **不要执行以下操作：**
> - ❌ 不要打开现有的 Uya 版本文件进行对比
> - ❌ 不要基于现有 Uya 版本进行修改
> - ❌ 不要参考现有 Uya 版本的实现逻辑
> 
> **应该执行的操作：**
> - ✅ 完全以 C 版本为唯一参考
> - ✅ 从头开始翻译整个函数
> - ✅ 翻译完成后，覆盖现有的 Uya 版本文件

### 步骤 5：重新覆盖式翻译代码

> **⚠️ 核心原则：完全按照 C 版本重新翻译，覆盖现有实现**

1. **完全按照 C 版本的逻辑重新翻译**（不要参考现有 Uya 版本，不要自己发挥）
   - 以 C 版本代码为唯一参考
   - 忽略现有 Uya 版本的实现
   - 从头开始翻译整个函数
   - 翻译完成后，完全覆盖现有的 Uya 版本代码
2. **注意类型系统差异**：
   - C 版本使用 `union`，Uya 版本使用结构体包含所有字段
     - C: `type.data.array.element_type`
     - Uya: `type.element_type`
   - C 版本使用 `->` 访问指针成员，Uya 版本使用 `.` 访问
     - C: `node->data.var_decl.name`
     - Uya: `node.var_decl_name`
   - C 版本使用 `*` 解引用，Uya 版本直接访问
     - C: `*array_type.data.array.element_type`
     - Uya: `array_type.element_type[0]`
   - C 版本使用 `const char *`，Uya 版本使用 `&byte`
     - C: `const char *name`
     - Uya: `name: &byte`
3. **处理指针运算**（⚠️ 重要）：
   - **Uya 不支持指针运算**，必须转换为数组访问
   - C: `ptr + i` → Uya: `&ptr[i]`
   - C: `*(ptr + i)` → Uya: `ptr[i]`
   - C: `ptr++` → Uya: 需要重构为数组访问或使用索引变量
   - 所有指针运算都必须转换为数组访问形式
4. **处理函数定义顺序**（⚠️ 重要）：
   - **Uya 不支持函数前向声明**，函数必须在使用前定义
   - 重新组织函数顺序，将函数定义放在使用之前
   - 对于需要相互调用的函数，使用函数表机制（如类型检查器的两遍检查）
5. **保持函数签名一致**：参数类型、返回值类型必须完全一致
6. **保持错误处理逻辑一致**：错误检查、错误报告、放宽检查的逻辑必须一致
7. **保持放宽检查的逻辑一致**：所有放宽检查的条件和返回值必须一致

### 步骤 6：编译和测试

1. 重新编译自举编译器：
   ```bash
   cd /media/winger/_dde_home/winger/uya/compiler-mini/uya-src
   ./compile.sh -e --c99
   ```

2. 运行测试：
   ```bash
   cd /media/winger/_dde_home/winger/uya/compiler-mini
   ./tests/run_programs.sh --uya
   ```

3. 检查测试结果：
   - 如果测试通过，**立即更新 TODO 文件**，标记对应函数为完成（`[ ]` → `[x]`）
   - 如果测试失败，继续调试，并在修复后更新 TODO

### 步骤 7：验证修复和更新 TODO

1. 对比 C 版本编译器的行为
2. 确保自举编译器行为一致
3. **⚠️ 立即更新 TODO 文件**：
   - 将对应函数的检查框标记为完成（`[ ]` → `[x]`）
   - 如果整个文件完成，更新文件的状态标记
   - 更新"翻译进度跟踪"部分的进度
   - 记录遇到的问题和解决方案（可选）

---

## 📝 翻译检查清单

### 对于每个函数，检查：

#### 函数签名
- [ ] 函数名是否一致
- [ ] 参数类型是否一致
- [ ] 参数数量是否一致
- [ ] 返回值类型是否一致

#### 逻辑流程
- [ ] 主要逻辑流程是否一致
- [ ] 条件判断是否一致
- [ ] 循环逻辑是否一致
- [ ] 递归调用是否一致

#### 错误处理
- [ ] 错误检查是否一致
- [ ] 错误报告是否一致
- [ ] 放宽检查的逻辑是否一致
- [ ] 错误返回是否一致

#### 类型系统
- [ ] 类型推断逻辑是否一致
- [ ] 类型比较逻辑是否一致
- [ ] 类型转换逻辑是否一致
- [ ] 类型创建逻辑是否一致

#### 数据结构访问
- [ ] 结构体字段访问是否正确（C 版本 `->` vs Uya 版本 `.`）
- [ ] 数组访问是否正确
- [ ] 指针解引用是否正确（C 版本 `*` vs Uya 版本直接访问）
- [ ] union 访问是否正确（C 版本 union vs Uya 版本结构体字段）
- [ ] **指针运算是否已转换为数组访问**（C 版本 `ptr + i` → Uya 版本 `&ptr[i]`）
- [ ] **函数定义顺序是否正确**（Uya 不支持前向声明，函数必须在使用前定义）

#### 符号表和函数表
- [ ] 符号表查找是否一致
- [ ] 符号表插入是否一致
- [ ] 函数表查找是否一致
- [ ] 函数表插入是否一致

#### 作用域管理
- [ ] 进入作用域逻辑是否一致
- [ ] 退出作用域逻辑是否一致
- [ ] 作用域级别管理是否一致

---

## 🎯 优先级说明

- **P0（最高）**：核心模块，必须优先翻译，影响编译器的基本功能
- **P1（高）**：重要模块，影响代码生成功能
- **P2（中）**：辅助模块，影响辅助功能

---

## 📊 翻译进度跟踪

> **⚠️ 重要提醒：** 每完成一个函数或文件的翻译，**必须立即更新**本部分的检查框和状态标记，确保进度跟踪准确。不要等到所有工作完成后再更新。

### 核心模块（P0）

- [ ] `checker.c` → `checker.uya` - ⚠️ 需要重新覆盖式翻译（完全按照 C 版本重新翻译，不要参考现有实现）
- [ ] `parser.c` → `parser.uya` - ⏳ 待翻译
- [ ] `lexer.c` → `lexer.uya` - ⏳ 待翻译
- [ ] `ast.c` → `ast.uya` - ⏳ 待翻译
- [ ] `arena.c` → `arena.uya` - ⏳ 待翻译
- [ ] `main.c` → `main.uya` - ⏳ 待翻译

### LLVM C API 代码生成模块（P1）

- [ ] `codegen/llvm_capi/main.c` → `codegen/llvm_capi/main.uya` - ⏳ 待翻译
- [ ] `codegen/llvm_capi/init.c` → `codegen/llvm_capi/init.uya` - ⏳ 待翻译
- [ ] `codegen/llvm_capi/function.c` → `codegen/llvm_capi/function.uya` - ⏳ 待翻译
- [ ] `codegen/llvm_capi/expr.c` → `codegen/llvm_capi/expr.uya` - ⏳ 待翻译
- [ ] `codegen/llvm_capi/stmt.c` → `codegen/llvm_capi/stmt.uya` - ⏳ 待翻译
- [ ] `codegen/llvm_capi/types.c` → `codegen/llvm_capi/types.uya` - ⏳ 待翻译
- [ ] `codegen/llvm_capi/structs.c` → `codegen/llvm_capi/structs.uya` - ⏳ 待翻译
- [ ] `codegen/llvm_capi/enums.c` → `codegen/llvm_capi/enums.uya` - ⏳ 待翻译
- [ ] `codegen/llvm_capi/global.c` → `codegen/llvm_capi/global.uya` - ⏳ 待翻译
- [ ] `codegen/llvm_capi/vars.c` → `codegen/llvm_capi/vars.uya` - ⏳ 待翻译
- [ ] `codegen/llvm_capi/funcs.c` → `codegen/llvm_capi/funcs.uya` - ⏳ 待翻译
- [ ] `codegen/llvm_capi/utils.c` → `codegen/llvm_capi/utils.uya` - ⏳ 待翻译

### 辅助模块（P2）

- [ ] `str_utils.c` → `str_utils.uya`（如果存在） - ⏳ 待翻译

---

## 🔍 常见翻译问题

### 1. 类型系统差异

**问题：** C 版本使用 `union`，Uya 版本使用结构体包含所有字段

**解决方案：**
- C 版本：`type.data.array.element_type`
- Uya 版本：`type.element_type`（直接访问字段）

### 2. 指针访问差异

**问题：** C 版本使用 `->` 访问指针成员，Uya 版本使用 `.` 访问

**解决方案：**
- C 版本：`node->data.var_decl.name`
- Uya 版本：`node.var_decl_name`（直接访问字段）

### 3. 指针解引用差异

**问题：** C 版本使用 `*` 解引用，Uya 版本直接访问

**解决方案：**
- C 版本：`*array_type.data.array.element_type`
- Uya 版本：`array_type.element_type[0]`（数组访问）

### 4. 指针运算差异（重要）

**问题：** Uya Mini **不支持指针运算**（如 `ptr + i`、`ptr++` 等），必须转换为数组访问形式

**解决方案：**
- C 版本指针运算：`ptr + i` → Uya 版本：`&ptr[i]`
- C 版本：`*(ptr + i)` → Uya 版本：`ptr[i]`
- C 版本：`ptr++` → Uya 版本：需要重构为数组访问或使用索引变量
- C 版本：`ptr[i]` → Uya 版本：`ptr[i]`（保持不变）

**示例：**
```c
// C 版本
Type *element_type_ptr = (Type *)arena_alloc(...);
element_type_ptr[0] = element_type;  // 或 *element_type_ptr = element_type
```

```uya
// Uya 版本
const element_type_ptr: &Type = arena_alloc(...) as &Type;
element_type_ptr[0] = element_type;  // 必须使用数组访问
```

**注意：**
- 所有指针运算都必须转换为数组访问
- 指针增量/减量操作需要重构为索引变量
- 指针比较（如 `ptr1 < ptr2`）需要转换为索引比较

### 5. 函数前向声明限制（重要）

**问题：** Uya Mini **不支持普通函数的前向声明**，函数必须在使用前定义

**解决方案：**
- **重新组织函数顺序**：将函数定义放在使用之前
- **使用函数表**：对于需要相互调用的函数，使用函数表机制（如 `checker_register_fn_decl`）
- **两遍检查**：第一遍注册函数签名，第二遍检查函数体（如类型检查器的实现）

**示例：**
```c
// C 版本：可以使用前向声明
static void function_b();
static void function_a() {
    function_b();  // 调用后定义的函数
}
static void function_b() {
    // ...
}
```

```uya
// Uya 版本：必须重新组织顺序
fn function_b() void {
    // ...
}

fn function_a() void {
    function_b();  // 现在 function_b 已经定义
}
```

**对于类型检查器等需要相互调用的函数：**
- 使用两遍检查机制：第一遍注册函数签名，第二遍检查函数体
- 参考 `checker_check` 函数的实现方式

### 6. 字符串处理差异

**问题：** C 版本使用 `const char *`，Uya 版本使用 `&byte`

**解决方案：**
- C 版本：`const char *name`
- Uya 版本：`name: &byte`

### 7. 错误处理差异

**问题：** C 版本可能使用不同的错误处理方式

**解决方案：**
- 保持错误处理逻辑完全一致
- 保持放宽检查的逻辑完全一致

---

## 📌 重要文件路径

### 源代码文件
- **C 版本源代码：** `compiler-mini/src/`
- **自举版本源代码：** `compiler-mini/uya-src/`
- **语法规范：** `compiler-mini/spec/UYA_MINI_SPEC.md`

### 构建和测试
- **自举编译器构建脚本：** `compiler-mini/uya-src/compile.sh`
- **测试脚本：** `compiler-mini/tests/run_programs.sh`
- **测试用例目录：** `compiler-mini/tests/programs/`
- **自举编译器可执行文件：** `compiler-mini/build/uya-compiler/compiler`

### 工作目录
- **项目根目录：** `/media/winger/_dde_home/winger/uya/compiler-mini`
- **自举编译器源代码：** `/media/winger/_dde_home/winger/uya/compiler-mini/uya-src`

---

## ⚠️ 注意事项

> **⚠️ 核心原则：重新覆盖式翻译**

1. **重新覆盖式翻译原则**：
   - **不要参考现有 Uya 版本的实现**：完全忽略 `uya-src/` 目录下的现有代码
   - **完全以 C 版本为唯一参考**：以 `src/` 目录下的 C 代码为唯一标准
   - **从头开始翻译**：不要基于现有代码修改，而是完全重新翻译
   - **覆盖式替换**：翻译完成后，完全覆盖现有的 Uya 版本文件

2. **始终参考语法规范：** 在翻译任何代码之前，必须先确认语法规范中的规则

3. **保持与 C 版本一致：** 所有逻辑都应该与 C 版本完全一致

4. **测试驱动翻译：** 每次翻译后都要运行测试验证

5. **记录翻译内容：** 在代码中添加注释说明翻译的要点

6. **⚠️ 指针运算限制：** Uya Mini 不支持指针运算，所有 `ptr + i` 必须转换为 `&ptr[i]`

7. **⚠️ 函数前向声明限制：** Uya Mini 不支持函数前向声明，必须重新组织函数顺序或使用函数表机制

8. **⚠️ 及时更新 TODO：** 每完成一个函数或文件的翻译，立即更新本文档中的检查框和状态标记，不要等到所有工作完成后再更新

---

## 📝 更新日志

**2024-01-XX：** 创建初始翻译 TODO 文档
- 列出了所有需要翻译的文件
- 提供了详细的工作流程和检查清单
- 记录了常见翻译问题和解决方案

---

**提示：** 
- **⚠️ 每次翻译完成后，必须立即更新本文档的"翻译进度跟踪"部分**，将对应函数的检查框标记为完成（`[ ]` → `[x]`），并更新文件状态。
- 建议记录完成状态和遇到的问题，便于后续参考和问题追踪。
- 不要等到所有工作完成后再更新，应该每完成一个函数就立即更新。

